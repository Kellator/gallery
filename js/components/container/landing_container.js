import React from 'react';
import { connect } from 'react-redux';
import PropTypes from 'prop-types';

import LandingPage from '../container/landing_container';

//component default login state.  if not authorized toggle to signup
//use connect and mapStateToProps - shold contain onClick/onSubmit/onChange handlers to pass as props
