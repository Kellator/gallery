//component for Gallery App = router would / be landing page or galleryapp?
import React from 'react';

export default class GalleryApp extends React.Component {
    render() {
        return (
            <div>
                <p>Welcome to the Gallery</p>
            </div>
        )
    }
}